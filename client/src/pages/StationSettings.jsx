import StationSettingsContainer from "../components/station-settings/StationSettingsContainer";

// Main entry point for the Station Settings page, rendering the StationSettingsContainer component.
export default function StationSettings() {
    return (
        <StationSettingsContainer />
    );
}