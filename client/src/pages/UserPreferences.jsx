import UserPreferencesContainer from "../components/user-preferences/UserPreferencesContainer";

// Main entry point for the User Preferences page, rendering the UserPreferencesContainer component.
export default function UserPreferences() {
    return (
        <UserPreferencesContainer />
    );
}