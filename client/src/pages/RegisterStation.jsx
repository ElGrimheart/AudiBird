import RegisterStationContainer from "../components/register-station/RegisterStationContainer";

// Main entry point for the Register Station page, rendering the RegisterStationContainer component.
export default function RegisterStation() {
    return (
        <RegisterStationContainer />
    );
}